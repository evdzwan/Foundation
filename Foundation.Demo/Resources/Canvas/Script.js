export function initialize(component, canvas) {
    // called when canvas is initialized
}

export function cleanup(component) {
    // called before canvas is disposed
}

export function resize(component, canvas) {
    // called whenever canvas is resized
}
